/* RESERVE multipack v1.0 — understands 6 x 1.5 l, 4 x 500 g and 10 eggs. */
(function(){
 const normUnit=u=>{u=String(u||'').toLowerCase();if(['stuck','stk','st','eier','ei','egg','eggs'].includes(u))return'stück';return u};
 function parse(value){let s=String(value||'').trim().toLowerCase().replace(/,/g,'.').replace(/[×*]/g,'x').replace(/\s+/g,' ');let m=s.match(/^(\d+(?:\.\d+)?)\s*x\s*(\d+(?:\.\d+)?)\s*(kg|g|l|ml|stück|stuck|stk|st)$/);if(m){let count=+m[1],v=+m[2],u=normUnit(m[3]);if(u==='kg'){v*=1000;u='g'}if(u==='l'){v*=1000;u='ml'}return{v:count*v,u,count,each:v,raw:value,multipack:true}}m=s.match(/^(\d+(?:\.\d+)?)\s*(eier|ei|egg|eggs|stück|stuck|stk|st)$/);if(m)return{v:+m[1],u:'stück',count:+m[1],each:1,raw:value,multipack:+m[1]>1};return null}
 function format(p){if(!p)return'';let v=Math.round(p.v*10)/10,u=p.u;if(u==='g'&&v>=1000)return(v/1000)+' kg';if(u==='ml'&&v>=1000)return(v/1000)+' l';return v+' '+u}
 function install(){const input=document.getElementById('scanQty');if(!input||input.dataset.multipack)return;input.dataset.multipack='1';input.placeholder='Menge, z. B. 500 g oder 6 × 1,5 l';const hint=document.createElement('div');hint.id='multipackHint';hint.className='small muted';hint.textContent='Mehrfachpackungen möglich: 6 × 1,5 l · 4 × 500 g · 10 Eier';input.insertAdjacentElement('afterend',hint);input.addEventListener('input',()=>{const p=parse(input.value);if(p?.multipack)hint.textContent=`Erkannt: ${p.count}er-Pack = ${format(p)} Gesamtbestand`;else hint.textContent='Mehrfachpackungen möglich: 6 × 1,5 l · 4 × 500 g · 10 Eier'})}
 function normalizeInput(value){const p=parse(value);return p?format(p):value}
 window.RESERVE_MULTIPACK={version:'1.0',parse,format,normalizeInput};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();