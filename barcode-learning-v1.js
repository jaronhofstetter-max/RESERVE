/* RESERVE barcode learning v1.3 — remembers corrections and automatically learns Open Food Facts product packshots. */
(function(){
 const KEY='reserveBarcodeProductsV1',$=id=>document.getElementById(id),digits=s=>String(s||'').replace(/\D/g,''),pending=new Map();
 function read(){try{const x=JSON.parse(localStorage.getItem(KEY)||'{}');return x&&typeof x==='object'?x:{}}catch{return{}}}
 function write(m){localStorage.setItem(KEY,JSON.stringify(m))}
 function iconFor(name,cat){return window.RESERVE_PRODUCT_ICONS?.iconFor?.({n:name,c:cat})||'🍽️'}
 function safeImage(value){value=String(value||'').trim();return /^https:\/\//i.test(value)?value:''}
 function rememberFinal(code,name,qty,cat,image){code=digits(code);name=String(name||'').trim();qty=String(qty||'').trim();if(!code||!name||!qty)return null;const m=read(),old=m[code]||{},icon=iconFor(name,cat),img=safeImage(image)||safeImage(old.image);m[code]={...old,code,name,qty,cat:cat||'Sonstiges',icon,image:img,foodState:'food',rememberedAt:new Date().toISOString(),learnedFromCorrection:true,trusted:true};write(m);return m[code]}
 async function learnImage(code){code=digits(code);if(!code)return'';const old=read()[code];if(safeImage(old?.image))return old.image;if(pending.has(code))return pending.get(code);const task=(async()=>{try{const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),3500);let r;try{r=await fetch('https://world.openfoodfacts.org/api/v2/product/'+encodeURIComponent(code)+'.json?fields=code,image_front_url,image_front_small_url,image_url',{headers:{Accept:'application/json'},signal:ctl.signal})}finally{clearTimeout(timer)}if(!r.ok)return'';const d=await r.json(),p=d?.product||{},image=safeImage(p.image_front_url)||safeImage(p.image_url)||safeImage(p.image_front_small_url);if(!image)return'';const m=read(),entry=m[code]||{code};m[code]={...entry,image,imageSource:'openfoodfacts',imageLearnedAt:new Date().toISOString()};write(m);let changed=false;try{const rows=JSON.parse(localStorage.getItem('reserveStock')||'[]');rows.forEach(row=>{if(String(row.barcode||'')===code&&!safeImage(row.image)){row.image=image;changed=true}});if(changed)localStorage.setItem('reserveStock',JSON.stringify(rows))}catch{}window.dispatchEvent(new CustomEvent('reserve:barcode-product-found',{detail:{code,image}}));window.dispatchEvent(new Event('reserve:stock-changed'));return image}catch{return''}finally{pending.delete(code)}})();pending.set(code,task);return task}
 function decorate(){const box=$('barcodeResult'),name=$('scanName')?.value?.trim(),cat=$('scanCat')?.value;if(!box||!name)return;let badge=box.querySelector('.reserve-scan-icon');if(!badge){badge=document.createElement('span');badge.className='reserve-scan-icon';badge.style.cssText='font-size:1.5rem;margin-right:.45rem;vertical-align:middle';const target=box.querySelector('.available-card b');if(target)target.prepend(badge)}if(badge)badge.textContent=iconFor(name,cat)+' ';const code=digits($('barcodeInput')?.value);if(code)learnImage(code)}
 function capture(){const code=digits($('barcodeInput')?.value),name=$('scanName')?.value,qty=$('scanQty')?.value,cat=$('scanCat')?.value;if(!code||!name||!qty)return;const old=read()[code];learnImage(code).then(image=>setTimeout(()=>rememberFinal(code,name,window.RESERVE_MULTIPACK?.normalizeInput?.(qty)||qty,cat,image||old?.image||''),0))}
 function canAutoAdd(code){const p=read()[digits(code)];return !!(p?.trusted&&p?.name&&p?.qty&&window.RESERVE_BARCODE?.parsedAmount?.(p.qty))}
 function autoAddKnown(code){code=digits(code);if(!canAutoAdd(code))return false;const input=$('barcodeInput');if(input)input.value=code;window.RESERVE_BARCODE?.lookup?.(code);let tries=0;const finish=()=>{tries++;const name=$('scanName')?.value?.trim(),qty=$('scanQty')?.value?.trim();if(name&&qty){window.RESERVE_BARCODE.addProduct();return}if(tries<20)setTimeout(finish,25)};setTimeout(finish,0);return true}
 function boot(){const add=$('scanAdd');if(add&&!add.dataset.barcodeLearning){add.dataset.barcodeLearning='1';add.addEventListener('click',capture,true)}const box=$('barcodeResult');if(box){new MutationObserver(decorate).observe(box,{childList:true,subtree:true})}['scanName','scanCat'].forEach(id=>$(id)?.addEventListener(id==='scanCat'?'change':'input',decorate));$('barcodeInput')?.addEventListener('change',()=>learnImage(digits($('barcodeInput')?.value)));decorate()}
 window.RESERVE_BARCODE_LEARNING={version:'1.3',rememberFinal,iconFor,read,canAutoAdd,autoAddKnown,learnImage};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
