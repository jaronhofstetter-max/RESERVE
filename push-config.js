// TenderHawk Web Push configuration.
// Public values only. Never place VAPID private keys or service-role secrets here.
window.TENDERHAWK_PUSH_CONFIG = {
  vapidPublicKey: '',
  subscribeEndpoint: ''
};
