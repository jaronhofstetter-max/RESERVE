import { chromium } from 'playwright';
import { spawn } from 'node:child_process';
const server=spawn('python3',['-m','http.server','4173'],{stdio:'ignore'});const sleep=ms=>new Promise(r=>setTimeout(r,ms));
try{await sleep(800);const browser=await chromium.launch({headless:true});const page=await browser.newPage();await page.goto('http://127.0.0.1:4173/index.html');await page.waitForFunction(()=>window.RESERVE_WEEKLY_PLAN&&document.getElementById('mealPlan'));const out=await page.evaluate(()=>{const x=RESERVE_WEEKLY_PLAN.build();return{days:x.days.length,slots:x.slots.length,times:x.days.map(d=>d.meals.map(m=>m.time)),version:RESERVE_WEEKLY_PLAN.version}});if(out.days!==7)throw new Error(`expected 7 days, got ${out.days}`);if(out.slots!==21)throw new Error(`expected 21 meal slots, got ${out.slots}`);const expected=['Frühstück','Mittagessen','Abendessen'];if(!out.times.every(x=>JSON.stringify(x)===JSON.stringify(expected)))throw new Error('meal times are not breakfast/lunch/dinner for every day');if(out.version!=='2.0')throw new Error('weekly planner version mismatch');console.log('Weekly Plan v2 E2E passed:',out);await browser.close()}finally{server.kill('SIGTERM')}
